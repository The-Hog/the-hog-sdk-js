# MatchMode

Match mode for LinkedIn keyword searches. exact searches the query as a quoted phrase; broad sends the query without phrase quotes.

## Example Usage

```typescript
import { MatchMode } from "@the-hog/sdk/models";

let value: MatchMode = "exact";
```

## Values

```typescript
"exact" | "broad"
```